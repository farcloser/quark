// Package buildctl provides container image building using buildctl (BuildKit CLI).
// It uses buildctl to communicate directly with a buildkitd daemon,
// bypassing the docker buildx abstraction layer.
package buildctl
