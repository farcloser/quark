// Package storage provides a minimalistic implementation that satisfies an append-only log system
// with mandatory signing.
// It is currently based on git, but could easily be interface-d and reimplemented with anything else that
// would qualify.
package storage
